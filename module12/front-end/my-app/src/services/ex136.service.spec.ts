/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Ex136Service } from './ex136.service';

describe('Service: Ex136', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Ex136Service]
    });
  });

  it('should ...', inject([Ex136Service], (service: Ex136Service) => {
    expect(service).toBeTruthy();
  }));
});
