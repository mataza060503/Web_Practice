/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Ex133Service } from './ex133.service';

describe('Service: Ex133', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Ex133Service]
    });
  });

  it('should ...', inject([Ex133Service], (service: Ex133Service) => {
    expect(service).toBeTruthy();
  }));
});
